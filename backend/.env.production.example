# Production Environment Variables for Railway Deployment
# Set these in your Railway service dashboard

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# Option 1: Use Railway's automatic DATABASE_URL (Recommended)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Option 2: Individual database credentials (if not using DATABASE_URL)
# DB_HOST=${{Postgres.PGHOST}}
# DB_PORT=${{Postgres.PGPORT}}
# DB_NAME=${{Postgres.PGDATABASE}}
# DB_USER=${{Postgres.PGUSER}}
# DB_PASSWORD=${{Postgres.PGPASSWORD}}

# JWT Secret (REQUIRED - Generate a strong random string)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_MIN_32_CHARS

# Email Configuration (Required for verification codes)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-specific-password

# CORS Origins (Your Vercel frontend URL)
# Include preview deployments with wildcard if needed
CORS_ORIGIN=https://your-app.vercel.app,https://your-app-*.vercel.app

# Google OAuth Configuration (Optional)
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_REDIRECT_URI=https://your-railway-app.up.railway.app/api/oauth/google/callback

# Frontend URL (Your Vercel deployment URL)
FRONTEND_URL=https://your-app.vercel.app

# Custom Domain Example (After setting up domains)
# CORS_ORIGIN=https://nuwendo.dev,https://www.nuwendo.dev
# GOOGLE_REDIRECT_URI=https://api.nuwendo.dev/api/oauth/google/callback
# FRONTEND_URL=https://nuwendo.dev
